# 职教智能体教学实训平台 — 完整技术文档

> 本文档面向接手开发的工程师，覆盖项目全貌、数据库设计、API接口、前端架构、AI集成和三步工作流。

---

## 一、项目总览

```
岗位能力生成/
├── manage.py                    # Django CLI 入口
├── .gitignore                   # 忽略规则（含 spark_assistant.py 保护密钥）
├── config/                      # Django 项目配置
│   ├── settings.py              # 数据库、模板、静态文件、中间件
│   ├── urls.py                  # 所有 URL 路由（页面 + API）
│   └── wsgi.py
├── chain/                       # 产业链 & 岗位 模块
│   ├── models.py                # Chain, Job
│   └── views.py                 # 页面渲染 + API（CRUD + AI生成）
├── crawl/                       # 爬虫 模块
│   ├── models.py                # CrawlTask, JobListing
│   └── views.py                 # 启动爬取 + 进度查询 API
├── ability/                     # 能力图谱 模块
│   ├── models.py                # AbilityMap + parse_abilities_to_tree()
│   └── views.py                 # 生成图谱 + 树查询 API
├── accounts/                    # 用户 模块（空壳，用 Django 内置 User）
├── frontend/                    # 前端（纯 HTML/CSS/JS）
│   ├── index.html               # 主布局（侧边栏 + iframe 框架）
│   ├── login.html               # 登录页（对接 Django Session 认证）
│   ├── 能力图谱库.html           # 核心功能页（三列思维导图 + 图谱下钻）
│   ├── 用户管理.html             # 用户管理（跳转 Django Admin）
│   ├── 角色管理.html             # 角色管理（静态展示）
│   └── components/
│       └── sidebar-menu.js      # 侧边栏 Web Component
├── data/                        # 数据库 + 旧数据缓存（gitignore）
│   └── db.sqlite3               # SQLite 数据库
├── output/                      # AI 生成的能力图谱 txt 导出（gitignore）
├── step1_gen_jobs.py            # Step 1: AI 生成岗位列表
├── step2_crawl_jobs.py          # Step 2: 爬取招聘数据
├── step3_gen_abilities.py       # Step 3: AI 生成能力图谱
├── spark_assistant.py           # 星辰Agent WebSocket 客户端（gitignore）
├── extract_abilities.py         # 备用 AI 客户端（OpenAI REST 兼容）
├── chain_workflow.py            # 原始 CLI 流水线（已弃用）
└── crawler.py / crawler_forum.py / batch_crawler.py  # 独立爬虫脚本
```

**技术栈**: Django 5.2 + SQLite + 纯原生 JS（无框架）+ 讯飞星辰Agent WebSocket

---

## 二、数据库设计（4 张核心表）

### ER 关系图

```
Chain (1) ────< (N) Job (1) ────< (N) CrawlTask (1) ────< (N) JobListing
                     │
                     │ OneToOne
                     ▼
                 AbilityMap
```

### 2.1 chain_chain（产业链表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BigAutoField (PK) | 自增主键 |
| `name` | CharField(100) **unique** | 产业链名称，如"智能制造" |
| `description` | CharField(500) blank | 描述 |
| `created_at` | DateTimeField auto_now_add | 创建时间 |

**Django ORM 用法**:
```python
from chain.models import Chain
chain = Chain.objects.create(name="新能源")
chain.jobs.all()  # 反向获取所有岗位
```

### 2.2 chain_job（岗位表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BigAutoField (PK) | 自增主键 |
| `chain` | **FK → Chain** CASCADE, related_name="jobs" | 所属产业链 |
| `name` | CharField(100) | 标准岗位名 |
| `aliases` | **JSONField** default=list | 别名列表，如 `["CNC操作工","数控车工"]` |
| `search_keywords` | **JSONField** default=list | 爬取搜索关键词 |
| `is_confirmed` | BooleanField default=False | 人工确认标记 |
| `created_at` | DateTimeField auto_now_add | 创建时间 |

**约束**: `unique_together = [("chain", "name")]`

### 2.3 crawl_crawltask（爬取任务表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BigAutoField (PK) | |
| `job` | FK → Job CASCADE, related_name="crawl_tasks" | 所属岗位 |
| `status` | CharField(20) choices | pending→running→completed/failed |
| `total_keywords` | IntegerField default=0 | 使用的搜索词数 |
| `total_results` | IntegerField default=0 | 爬到的招聘信息总数 |
| `results_json` | JSONField default=list | 原始结果（实际未使用，数据存 JobListing） |
| `created_at` | DateTimeField auto_now_add | |

### 2.4 crawl_joblisting（招聘信息表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BigAutoField (PK) | |
| `task` | FK → CrawlTask CASCADE, related_name="listings" | |
| `title` | CharField(200) | 岗位名称 |
| `company` | CharField(200) | 公司 |
| `city` | CharField(100) blank | 城市 |
| `salary` | CharField(100) blank | 薪资 |
| `education` | CharField(50) blank | 学历要求 |
| `requirements` | **TextField** blank | **任职要求（AI 提取的核心输入）** |
| `headcount` | CharField(20) blank | 招聘人数 |
| `post_date` | CharField(50) blank | 发布日期 |
| `source` | CharField(100) blank | 来源（"中国公共招聘网"） |

### 2.5 ability_abilitymap（能力图谱表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BigAutoField (PK) | |
| `job` | **OneToOneField → Job** CASCADE, related_name="ability_map" | 每个岗位唯一 |
| `abilities_json` | **JSONField** default=list | 解析后的树结构 |
| `total_abilities` | IntegerField default=0 | 一级能力项数量 |
| `total_skills` | IntegerField default=0 | 叶子技能点总数 |
| `raw_text` | TextField blank | AI 原始输出文本 |
| `created_at` | DateTimeField auto_now_add | 生成时间 |

**abilities_json 数据结构**（2 层树）:
```json
[
  {
    "name": "数控车床操作",
    "children": [
      {"name": "使用广数系统完成对刀操作精度±0.01mm"},
      {"name": "使用FANUC系统编制车削加工程序"},
      ...
    ]
  },
  ...
]
```

**生成方式**: AI 返回 `能力名---技能1/技能2/技能3` 格式文本，经 `parse_abilities_to_tree()` 解析存入。

### 2.6 认证体系

使用 Django 内置 `auth.User` 表。`accounts` app 无自定义模型。角色信息在前端硬编码为 `"专业负责人"`，无数据库角色表。

---

## 三、API 接口完整参考

所有 API 均 `@csrf_exempt`，无 `@login_required`。基础路径: `http://127.0.0.1:8000`

### 3.1 认证 API（config/urls.py 内联定义）

| 方法 | 路径 | 请求体 | 响应 | 说明 |
|------|------|--------|------|------|
| POST | `/api/auth/login` | `{"username":"", "password":""}` | `{"ok":true, "user":{...}}` | Session 登录 |
| GET | `/api/auth/logout` | - | `{"ok":true}` | 登出 |
| GET | `/api/auth/me` | - | `{"name":"", "role":"专业负责人"}` | 当前用户信息 |

### 3.2 产业链 & 岗位 API（chain/views.py）

| 方法 | 路径 | 请求体 | 响应 | 说明 |
|------|------|--------|------|------|
| GET | `/api/chain/list` | - | `{"chains":[{id,name,job_count,created_at}]}` | 列表 |
| POST | `/api/chain/create` | `{"name":"智能制造"}` | `{"chain_id":1, "jobs":[...]}` | **同步**调 AI 生成岗位 |
| GET | `/api/chain/<id>/detail` | - | `{id,name,jobs:[...]}` | 含嵌套岗位 |
| DELETE | `/api/chain/<id>/delete` | - | `{"deleted":true}` | 级联删除 |
| POST | `/api/job/create` | `{"chain_id":1, "name":"电工"}` | `{"id":5, "name":"电工"}` | 手动加岗位 |
| POST | `/api/job/<id>/update` | `{"aliases":[], "search_keywords":[], ...}` | `{id,name,is_confirmed}` | 部分更新 |
| DELETE | `/api/job/<id>/delete` | - | `{"deleted":true}` | 删除岗位 |

### 3.3 爬虫 API（crawl/views.py）

| 方法 | 路径 | 请求体 | 响应 | 说明 |
|------|------|--------|------|------|
| POST | `/api/crawl/start` | `{"job_id":1}` | `{"task_id":5, "status":"pending"}` | 启动**后台线程**爬取 |
| GET | `/api/crawl/<task_id>/status` | - | `{"task_id":5,"status":"running/completed","total_results":133}` | 轮询进度 |

### 3.4 能力图谱 API（ability/views.py）

| 方法 | 路径 | 请求体 | 响应 | 说明 |
|------|------|--------|------|------|
| POST | `/api/ability/generate` | `{"job_id":1}` | `{"status":"processing"}` | 启动后台线程生成 |
| GET | `/api/ability/<job_id>/tree` | - | `{job_name, total_abilities, total_skills, abilities:[...], status:"ready"/"not_generated", crawl_status}` | 获取树 + 爬取状态 |
| GET | `/api/ability/<job_id>/text` | - | `{"text":"..."}` | 原始 AI 文本 |

### 3.5 页面路由

| 路径 | 视图 | 说明 |
|------|------|------|
| `/` | `index_view` | 未登录→login.html |
| `/login.html` | `login_view` | 登录页 |
| `/index.html` | `index_view` | 主框架（需登录） |
| `/能力图谱库.html` 或 `/chain-list.html` | `page_chain_list` | 核心功能页 |
| `/用户管理.html` 或 `/user-list.html` | `page_user_list` | |
| `/角色管理.html` 或 `/role-list.html` | `page_role_list` | |
| `/admin/` | Django admin | CRUD 所有表 |

---

## 四、前端架构

### 4.1 整体结构（iframe SPA）

```
index.html（壳）
├── <sidebar-menu> Web Component（220px 固定左侧栏）
│   ├── 渐变头部 "教育实训"
│   ├── 能力图谱组 → 能力图谱库
│   ├── 系统管理组 → 用户管理 / 角色管理
│   └── 用户区（头像 + 退出登录）
└── <iframe id="contentFrame">（主内容区）
    ├── 能力图谱库.html（启动默认加载）
    ├── 用户管理.html
    └── 角色管理.html
```

**路由机制**: 点击侧边栏 → `sidebar-menu.js` 派发 `menu-select` CustomEvent → `index.html` 监听，设 `contentFrame.src = href`

### 4.2 侧边栏组件（sidebar-menu.js）

**Web Component**, 通过 `<sidebar-menu>` 标签使用。

**公开方法**:
- `setMenuData(data)` — 设置菜单
- `getMenuData()` — 获取菜单

**菜单数据格式**:
```javascript
[
  {
    group: "能力图谱",           // 组标签
    items: [{
      label: "能力图谱",          // 菜单项文本
      icon: "network",           // 图标 key（内置 SVG）
      open: true,                // 默认展开
      children: [
        { label: "能力图谱库", href: "能力图谱库.html", active: true }
      ]
    }]
  }
]
```

**CustomEvents**（冒泡到 index.html）:
- `menu-select` detail=`{label, href}` → 切换 iframe
- `profile` detail=`{}` → 跳转个人中心
- `logout` → 跳转 login.html

### 4.3 核心页面（能力图谱库.html）

**两套 CSS 变量系统**:
- 蓝色系（index/login/用户/角色）：`--blue-primary: #3B82F6`
- 图谱系（能力图谱库）：`--purple/#6867f2`、`--cyan/#12b7ee`、`--orange/#f4a51c`、`--rose/#ec5a8d`、`--green/#23c18a`

**两视图**:

**Level 1 — 能力体系层级管理**（三列横向思维导图）:
```
┌────────────┬──────────────┬────────────────┐
│ 产业链      │ 职业岗位       │ 岗位能力         │
│ [搜索]     │ [搜索]        │ [搜索]           │
│            │              │                │
│ ■ 智能制造  │──■ 数控车床   │──■ 数控车床操作   │
│   [新增岗位]│    [查看图谱]  │    [查看]        │
│            │──■ 工业机器人 │──■ PLC编程       │
│            │    [①爬取]    │                │
└────────────┴──────────────┴────────────────┘
```
**SVG 曲线连线**: `connection-layer` 里动态生成 `<path>` 贝塞尔曲线

**Level 2 — 岗位能力详情**（下钻）:
```
┌──────────┬──────────┬────────────────┐
│ 岗位能力   │ 能力单元   │ 知识点/技能点    │
│          │          │                │
│ ■ 数控操作 │──■ 单元1 │──■ 技能1       │
│          │          │──■ 技能2       │
│          │──■ 单元2 │──■ 技能3       │
└──────────┴──────────┴────────────────┘
```

**关键 JavaScript 全局函数**:
- `loadAllData()` — 加载全部数据入口
- `renderLevel1()` / `renderLevel2()` — 渲染两视图
- `toggleNode(id)` / `toggleAllNodes()` — 节点展开收缩
- `filterColumn(type, keyword)` — 列搜索过滤
- `drillToLevel2(jobId, name, abilityFilter)` — Level 1→2 下钻
- `openChainModal()` — 新增产业链弹窗
- `aiGenerateChain(btn)` — AI 分析生成岗位
- `startCrawlForJob(jobId, btn)` — 爬取数据+轮询
- `genAbilityForJob(jobId, btn)` — 生成图谱+轮询
- `drawConnections()` — 绘制 SVG 连线

**三步按钮状态机**:
```
待爬取 → [① 爬取数据]
已爬取 → [② 生成图谱]  
已生成 → [查看图谱]
```

---

## 五、AI 集成架构

### 5.1 spark_assistant.py（核心 AI 客户端）

**连接方式**: WebSocket，非 REST HTTP  
**目标**: `wss://spark-openapi.cn-huabei-1.xf-yun.com/v1/assistants/nq8dw1cpxg31_v1`  
**认证**: HMAC-SHA256 签名 URL  
**超时**: 300 秒（5 分钟）  
**密钥**: 硬编码在产品代码中，`.gitignore` 排除此文件

**核心函数**:
```python
from spark_assistant import call_assistant
text = call_assistant(prompt, temperature=0.5, max_tokens=4096)
```

**消息格式**（标准星火 WS 协议）:
```json
{
  "header": {"app_id": "501527a1", "uid": "django_platform"},
  "parameter": {"chat": {"domain": "generalv3.5", "temperature": 0.5, "max_tokens": 4096}},
  "payload": {"message": {"text": [{"role": "user", "content": "提示词内容"}]}}
}
```

### 5.2 step1_gen_jobs.py（岗位生成）

**串联**: 前端→`POST /api/chain/create`→`chain/views.py`→`step1_gen_jobs.gen_jobs()`→`spark_assistant.call_assistant()`

**提示词要点**:
- 角色定位：职业教育岗位分析专家
- 约束：[OK] 操作员/技工/维修工 [NO] 研发工程师
- 学历：中专/大专/高职
- 输出：10-20 个岗位，严格 JSON，含 name、aliases、search_keywords

### 5.3 step3_gen_abilities.py（能力图谱生成）

**串联**: 前端→`POST /api/ability/generate`→后台线程→`_run_ability_gen()`→`step3_gen_abilities.gen_ability()`→`spark_assistant.call_assistant()`

**数据处理**: 收集 JobListing.requirements → 去重（前40字） → 截断（300字/条） → 合并（≤14000字）

**提示词要点**:
- 角色：15 年一线带徒经验的企业培训师
- 输出格式：`能力名---技能1/技能2/技能3`（每行一个能力）
- 技能三要素：[工具]+[操作]+[标准/精度]
- 每个能力 5-10 个技能点，15-30 项能力

---

## 六、三步工作流详解

```
Step 1: AI生成岗位
  输入：产业链名称（如"新能源"）
  调用：POST /api/chain/create → step1_gen_jobs.gen_jobs()
  输出：Chain + N个Job（含search_keywords）
  方式：同步（阻塞 HTTP 请求）

Step 2: 爬取招聘数据
  输入：岗位 search_keywords
  调用：POST /api/crawl/start → 后台线程 → step2_crawl_jobs.crawl_single_job()
  目标：job.mohrss.gov.cn（中国公共招聘网）
  方式：POST 表单 → 解析隐藏 JSON → 去重 → 存 JobListing
  状态：轮询 GET /api/crawl/<task_id>/status

Step 3: AI生成能力图谱
  前提：Step 2 完成（有 crawl_task status=completed）
  输入：JobListing.requirements 集合
  调用：POST /api/ability/generate → 后台线程 → step3_gen_abilities.gen_ability()
  输出：AbilityMap（abilities_json + raw_text）
  状态：轮询 GET /api/ability/<job_id>/tree
```

---

## 七、关键配置速查

| 配置项 | 值 |
|--------|-----|
| Django 版本 | 5.2.5 |
| Python | 3.13 (conda base) |
| 数据库 | SQLite `data/db.sqlite3` |
| 端口 | 8000 |
| 启动命令 | `python manage.py runserver 0.0.0.0:8000` |
| 管理员账号 | admin / admin123 |
| 中文 URL | 支持（如 `/能力图谱库.html`）同时有英文别名 |
| 静态文件 | `/static/...` → `frontend/` |
| 模板目录 | `frontend/` |

---

## 八、已知问题 & 可改进点

1. **API 无鉴权**：所有数据 API 均 `@csrf_exempt` 且无 `@login_required`，任何人都能调用
2. **后台任务脆弱**：使用 `daemon=True` 线程，服务器重启会丢失进行中的任务
3. **无任务队列**：爬虫和 AI 调用无并发控制，可能同时启动多个耗资源的操作
4. **`/api/chain/create` 同步阻塞**：AI 调用在请求线程内完成，可能需要等待数分钟，与其他 API 不一致
5. **角色硬编码**：用户角色在前端固定为"专业负责人"，无后端角色管理
6. **Django Admin 未注册模型**：四个 app 的 `admin.py` 均为空，无法在后台管理自定义模型
7. **前端无错误恢复**：后台线程失败时，前端轮询可能永远等待

---

## 九、后续开发指南

### 如果要加新页面
1. 在 `frontend/` 创建 `新页面.html`
2. 在 `config/urls.py` 添加 `path("新页面.html", page_xxx)`
3. 在 `index.html` 的 `setMenuData()` 中加入菜单项
4. 页面内用 `fetch("/api/...")` 调现有 API

### 如果要换 AI 大模型
1. 用 DeepSeek/通义千问等 → 改 `spark_assistant.py` 的 `call_assistant()` 为 HTTP REST 调用
2. 或者在星辰Agent 平台修改智能体配置，不需要改代码

### 如果要改数据结构
1. 改 `models.py` → `python manage.py makemigrations` → `python manage.py migrate`
2. 同步更新对应的 `views.py` 和前端 JS

### 启动检查清单
```bash
# 1. 确认 conda 环境有 websocket-client
pip list | grep websocket

# 2. 确认数据库存在
ls data/db.sqlite3

# 3. 启动
python manage.py runserver 0.0.0.0:8000

# 4. 浏览器打开 http://127.0.0.1:8000 → admin/admin123 登录
```
